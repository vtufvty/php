<?php
    $a = 5;
    $b = '05';
    var_dump($a == $b);         // Потому равны и имеют тот же тип 
    var_dump((int)'012345');     // Потому что int целое число.
    var_dump((float)123.0 === (int)123.0); // Потому что типы данных разные
    var_dump((int)0 === (int)'hello, world'); // Потому что и там и там int
?>